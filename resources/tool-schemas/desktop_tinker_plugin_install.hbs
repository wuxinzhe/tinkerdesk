{
  "name": "desktop_tinker_plugin_install",
  "description": "Install a plugin from a local folder, a local .zip file, or a remote download URL. Returns the installed plugin info. For install workflow details (reading the plugin's guide.md, preparing dependencies), follow the tinkerdesk-plugin-install skill.",
  "parameters": {
    "type": "object",
    "properties": {
      "source": { "type": "string", "description": "Plugin source: local folder path, local .zip path, or http(s) download URL" }
    },
    "required": ["source"]
  },
  "toolType": "desktop",
  "emoji": "🧩"
}
