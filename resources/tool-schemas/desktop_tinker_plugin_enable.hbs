{
  "name": "desktop_tinker_plugin_enable",
  "description": "Enable (register) a plugin. Runs the plugin's check() self-check first; if it fails, the result includes failing checks with hints for what to fix (download models, configure).",
  "parameters": {
    "type": "object",
    "properties": {
      "id": { "type": "string", "description": "Plugin id (from plugin_list)" }
    },
    "required": ["id"]
  },
  "toolType": "desktop",
  "emoji": "✅"
}
