{
  "name": "desktop_tinker_plugin_configure",
  "description": "Read a plugin's config schema and current values; pass values to save configuration. Use after installing a plugin to auto-fill detected environment paths (e.g. python path, model dir) before enabling.",
  "parameters": {
    "type": "object",
    "properties": {
      "id": { "type": "string", "description": "Plugin id (from plugin_list)" },
      "values": { "type": "object", "description": "Optional config values to save (omit to just read schema + current config)" }
    },
    "required": ["id"]
  },
  "toolType": "desktop",
  "emoji": "⚙️"
}
