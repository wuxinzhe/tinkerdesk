/**
 * constants.ts — 旧入口（保留兼容，重定向到统一常量包 core/constants）
 *
 * 全部常量已迁移到 core/constants/（对齐 showing-agent core/constant 包），
 * 此处 re-export 保持既有 import 不破坏。新代码请从 core/constants 导入。
 */
export * from '../constants'
