{
  "name": "stt",
  "description": "Convert an audio file to text (speech recognition). Provider is user-configured (extension providers like sherpa local), not model-selected. Returns the transcribed text.",
  "parameters": {
    "type": "object",
    "properties": {
      "file_path": {
        "type": "string",
        "description": "Absolute path to the audio file to transcribe (wav/mp3/ogg)."
      }
    },
    "required": ["file_path"]
  },
  "toolType": "desktop",
  "supportsProvider": true,
  "emoji": "🎤"
}
