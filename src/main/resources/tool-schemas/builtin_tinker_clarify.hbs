{
  "name": "builtin_tinker_clarify",
  "description": "Ask the user for clarification when you need more information or they need to choose from multiple options. Presents a structured question with answer choices for the user to pick from.",
  "parameters": {
    "type": "object",
    "properties": {
      "question": {
        "type": "string",
        "description": "The clarification question to present to the user"
      },
      "choices": {
        "type": "array",
        "description": "Answer choices for the user to pick from. Do NOT include an 'Other'/'其他' option — the UI automatically adds one for users to type their own answer.",
        "items": {
          "type": "string"
        }
      }
    },
    "required": ["question"]
  },
  "emoji": "❓"
}
