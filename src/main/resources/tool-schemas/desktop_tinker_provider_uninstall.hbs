{
  "name": "desktop_tinker_provider_uninstall",
  "description": "Uninstall a provider: stops it, unregisters its providers, and deletes the provider directory including downloaded models. Irreversible.",
  "parameters": {
    "type": "object",
    "properties": {
      "id": { "type": "string", "description": "Provider id (from provider_list)" }
    },
    "required": ["id"]
  },
  "toolType": "desktop",
  "emoji": "🗑️"
}
