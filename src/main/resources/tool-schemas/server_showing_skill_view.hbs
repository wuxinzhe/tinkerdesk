{
  "name": "server_showing_skill_view",
  "description": "Load a skill's full content. Skills are recorded in the database and accessible by name. Returns the skill's metadata, description, body, config, triggers, and other fields. Use skills_list first to discover available skill names.",
  "parameters": {
    "type": "object",
    "properties": {
      "name": { "type": "string", "description": "The skill name (use skills_list to see available skills)." }
    },
    "required": ["name"]
  },
  "toolType": "server",
  "emoji": "📘"
}
