{
  "name": "desktop_tinker_close_terminal",
  "description": "Close the read-only terminal view for one of your background processes (the views mirroring terminal(background=true) runs). This does NOT kill the process — it only drops the view; the output keeps buffering and remains available via process/read_terminal. Use it to tidy up when a background process's live terminal is no longer worth tinker. To actually stop the process, use process(action='kill') instead.",
  "parameters": {
    "type": "object",
    "properties": {
      "process_id": { "type": "string", "description": "The background process's session id (from terminal(background=true) output or process(action='list')) whose view should be closed." }
    },
    "required": ["process_id"]
  },
  "toolType": "desktop",
  "emoji": "🔌"
}
