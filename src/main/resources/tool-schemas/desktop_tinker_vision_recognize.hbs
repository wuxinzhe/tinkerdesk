{
  "name": "desktop_tinker_vision_recognize",
  "description": "Analyze images using a multimodal vision model. Accepts one or more image URLs (http/https/data: base64) plus an optional text prompt/question. Returns the model's textual analysis of the image(s). Use this when the user asks about the content of an image, screenshot, or visual data.",
  "parameters": {
    "type": "object",
    "properties": {
      "image_url": {
        "type": "string",
        "description": "The image URL to analyze (http/https/data: base64 data URL)."
      },
      "prompt": {
        "type": "string",
        "description": "Optional question or instruction about the image. Default: '描述这张图片'"
      }
    },
    "required": ["image_url"]
  },
  "toolType": "desktop",
  "emoji": "👁️",
  "supportsProvider": true
}
